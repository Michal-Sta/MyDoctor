import React from "react";

interface ProfileProps {
  date: string;
}

export const Profile = () => {
  return (
    <div className="container">
      <div className="alert-section">Profile</div>
    </div>
  );
};
