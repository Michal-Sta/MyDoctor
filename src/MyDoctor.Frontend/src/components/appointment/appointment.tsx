import React from "react";

interface AppointmentProps {
  date: string;
}

export const Appointment = (props: AppointmentProps) => {
  return (
    <div className="container">
      <div className="alert-section">Test</div>
    </div>
  );
};
